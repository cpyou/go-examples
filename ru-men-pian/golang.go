package ru_men_pian

func sample1() {
	println(12334)
	// 创建栈
	//stack := make([]int, 0)
	//// push压入
	//stack = append(stack, 10)
	//// pop弹出
	//v := stack[len(stack)-1]
	//println(v)
	//stack = stack[:len(stack)-1]
	//// 检查栈空
	//println(len(stack))
	//len(stack) == 0
}
